.row-fluid.search_row
  .span4
    %h1
      .rainbow
        = link_to "SPRINKLES", root_path
  - if current_user
    .span5.offset2
      .wide_form
        = form_tag(search_path, method: :post) do 
          = text_field_tag :tag, "search by sprinkle"
          .search_button
            = submit_tag "Search"